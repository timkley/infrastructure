# Pin the OpenBao image tag. Check Docker Hub/OpenBao release notes before bumping.
OPENBAO_VERSION=2.5.5

# Keep OpenBao off the public interface. Use Lando's Tailscale IP here only when
# direct tailnet access is desired; otherwise keep localhost and use SSH forwarding.
OPENBAO_BIND_ADDR=127.0.0.1
